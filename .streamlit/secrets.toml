# ⚠️ NEVER commit real secrets here.
# On Streamlit Cloud, set these in the app's Secrets dashboard instead.
# Locally, fill in your keys below but make sure .streamlit/secrets.toml is in .gitignore.

OPENROUTER_API_KEY = "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"

# Firebase credentials should be set as a [firebase] section here locally,
# or via the Streamlit Cloud Secrets dashboard for deployment.
